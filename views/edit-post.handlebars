<article>
    <a href="/dashboard"> &larr; Back to Dashboard</a>
    <h2>
        Edit Post
    </h2>
    <form class="edit-post-form">
        <div>
            <input name="post-title" type="text" value="{{post.title}}" />
            <input name="post-body" type="text" value="{{post.post_body}}" />
        </div>
        <div>
            <p class="post" >Posted by you on {{format_date post.created_at}}</p>
            <a href="/post{{post.id}}" class="comment-count">{{post.comments.length}}</a>
        </div>
        <button class="buttons" type="submit" >Save Post</button>
        <button class="delete-post-btn" type="button" > Delete Post</button>
    </form>
    
<form class="comment-form">
    <div> 
        <textarea name="comment-body"></textarea>
    </div>
    <div>
        <button type="submit">Add Comment</button>
    </div>
</form>

{{> comments post.comments}}
</article>
<script src="/javascript/edit-post.js"></script>
<script src="/javascript/delete-post.js"></script>
<script src="/javascript/comment.js"></script>
